#include<stdio.h>
int fac(int n)
{
    if(n==0)
        return 1;
}
int fac1(int n)
{
    int f=1, i;
    for(i=1; i<=n; i++)
    {
        f=f*i;
    }
    return f;
}
int main()
{
    int n;
    printf("Enter the value of n: ");
    scanf("%d", &n);
    int r=fac1(n);
    printf("%d", r);
}
